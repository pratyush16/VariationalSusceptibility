
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Portugal - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Portugal'
      model: 'Hetsus'
       LogL: 2.2971e+05
          d: [0.3039 0.3065 0.3090]
          p: [0.6961 0.6935 0.6910]
    inidist: [7.3188 7.7178 8.1258]
       R0_1: [4.1792 4.2567 4.3262]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [4.1931 4.2550 4.3120]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 3
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 