
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Belgium - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Belgium'
      model: 'Hetsus'
       LogL: 4.0306e+05
          d: [0.3907 0.3934 0.3984]
          p: [0.6093 0.6066 0.6016]
    inidist: [16.0327 16.8529 16.8987]
       R0_1: [3.3383 3.3410 3.4114]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [3.1200 3.1334 3.1710]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 1
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 