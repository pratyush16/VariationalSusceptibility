
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Portugal - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> Last Error in function barrier, line 0, file /home1/apps/matlab/2020a/toolbox/optim/optim/barrier.p.
>> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Portugal'
      model: 'Hetsus'
       LogL: 2.2963e+05
          d: [0.2141 0.2163 0.2187]
          p: [0.7859 0.7837 0.7813]
    inidist: [9.6862 9.8279 10.0156]
       R0_1: [3.9827 4.0209 4.0535]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [0.0523 0.1061 0.2151]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 3
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 