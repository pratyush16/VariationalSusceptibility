
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Portugal - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> Last Error in function barrier, line 0, file /home1/apps/matlab/2020a/toolbox/optim/optim/barrier.p.
>> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Portugal'
      model: 'Hetsus'
       LogL: 2.2935e+05
          d: [0.2113 0.2143 0.2168]
          p: [0.7887 0.7857 0.7832]
    inidist: [8.3066 8.5333 8.7433]
       R0_1: [4.1673 4.2110 4.2650]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [0.0510 0.0764 0.1658]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 3
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 