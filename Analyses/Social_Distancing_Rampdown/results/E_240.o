
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
England - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> Last Error in function barrier, line 0, file /home1/apps/matlab/2020a/toolbox/optim/optim/barrier.p.
>> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'England'
      model: 'Hetsus'
       LogL: 1.8063e+06
          d: [0.4140 0.4152 0.4164]
          p: [0.5860 0.5848 0.5836]
    inidist: [43.9796 44.1850 44.3569]
       R0_1: [3.0794 3.0917 3.1057]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [1.4926 1.5019 1.5122]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 32
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 