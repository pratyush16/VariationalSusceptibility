
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
England - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> Last Error in function barrier, line 0, file /home1/apps/matlab/2020a/toolbox/optim/optim/barrier.p.
Last Error in function barrier, line 0, file /home1/apps/matlab/2020a/toolbox/optim/optim/barrier.p.
>> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'England'
      model: 'Hetsus'
       LogL: 1.8041e+06
          d: [0.3441 0.3455 0.3470]
          p: [0.6559 0.6545 0.6530]
    inidist: [46.1906 46.3029 46.4034]
       R0_1: [3.0021 3.0076 3.0134]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [0.5875 0.6157 0.6437]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 32
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 