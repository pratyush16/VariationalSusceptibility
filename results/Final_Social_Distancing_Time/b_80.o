
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Belgium - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Belgium'
      model: 'Hetsus'
       LogL: 4.0797e+05
          d: [0.3262 0.3280 0.3294]
          p: [0.6738 0.6720 0.6706]
    inidist: [10.8521 10.9746 11.1528]
       R0_1: [3.8362 3.8537 3.8697]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [2.7692 2.7875 2.8002]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 1
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 