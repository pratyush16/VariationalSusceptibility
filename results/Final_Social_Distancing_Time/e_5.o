
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
England - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'England'
      model: 'Hetsus'
       LogL: 1.7945e+06
          d: [0.5805 0.5816 0.5827]
          p: [0.4195 0.4184 0.4173]
    inidist: [50.0269 50.0414 50.0898]
       R0_1: [2.7117 2.7143 2.7166]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [2.2136 2.2174 2.2346]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 32
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 