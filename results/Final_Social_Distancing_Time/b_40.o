
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Belgium - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Belgium'
      model: 'Hetsus'
       LogL: 4.0719e+05
          d: [0.3254 0.3279 0.3305]
          p: [0.6746 0.6721 0.6695]
    inidist: [12.5010 12.5790 12.8571]
       R0_1: [3.6603 3.6825 3.6980]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [2.6773 2.7062 2.7167]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 1
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 