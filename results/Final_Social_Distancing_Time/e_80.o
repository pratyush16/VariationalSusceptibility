
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
England - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'England'
      model: 'Hetsus'
       LogL: 1.8070e+06
          d: [0.4726 0.4736 0.4746]
          p: [0.5274 0.5264 0.5254]
    inidist: [42.4401 42.5529 42.5986]
       R0_1: [3.1211 3.1267 3.1340]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [1.8993 1.9040 1.9095]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 32
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 