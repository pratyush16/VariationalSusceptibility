
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
Belgium - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'Belgium'
      model: 'Hetsus'
       LogL: 4.0625e+05
          d: [0.2954 0.3026 0.3042]
          p: [0.7046 0.6974 0.6958]
    inidist: [14.1481 14.2316 14.9306]
       R0_1: [3.4573 3.5205 3.5296]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [2.3585 2.4255 2.4344]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 1
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 