
                            < M A T L A B (R) >
                  Copyright 1984-2020 The MathWorks, Inc.
                  R2020a (9.8.0.1323502) 64-bit (glnxa64)
                             February 25, 2020

 
To get started, type doc.
For product information, visit www.mathworks.com.
 
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 

********************************************>> 
England - Hetsus
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
MCMC
>> >> >> >> >> >> >> >> -> Sampling FINISHED.
>> >> -> Calculation of confidence intervals for parameters FINISHED.
>> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> 
Final_results = 

  struct with fields:

    country: 'England'
      model: 'Hetsus'
       LogL: 1.8060e+06
          d: [0.4722 0.4731 0.4757]
          p: [0.5278 0.5269 0.5243]
    inidist: [44.2382 44.3504 44.7020]
       R0_1: [2.9986 3.0170 3.0264]
       R0_2: []
       R0_3: []
       R0_4: []
       CV_1: [1.8178 1.8355 1.8391]
       CV_2: []
       CV_3: []
       CV_4: []
      lag_1: 32
      lag_2: []
      lag_3: []
      lag_4: []

>> >> >> >> >> >> >> 